import 'package:flutter/material.dart';

class AppColors {
  // Couleur principale pour les titres, onglets et sliders
  static const mainBlue = Color(0xFF1B3B5A); // Bleu/vert foncé avec une touche de rouge

  // Couleur pour les cadres
  static const cardBlue = Color.fromRGBO(10, 17, 40, 0.5); // Bleu nuit avec opacité 0.5

  // Nouvelle couleur AppBar (du sélecteur de couleur)
  static const appBarColor = Color(0xFF3F3F52); // RGB: 63, 63, 82 - Couleur exacte du sélecteur

  // Couleurs de texte
  static const lightText = Colors.white;
  static const darkText = Color(0xFF0A1128);

  // Couleurs de fond
  static const lightBackground = Colors.white;
  static const darkBackground = Color(0xFF0A1128);
} 