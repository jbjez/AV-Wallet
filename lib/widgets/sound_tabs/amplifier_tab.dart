import 'package:flutter/material.dart';

class AmplifierTab extends StatelessWidget {
  const AmplifierTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Amplifier Tab Content'),
    );
  }
}